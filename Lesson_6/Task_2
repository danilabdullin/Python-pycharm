class Road:
    def __init__(self, length, width):
        self._length = length
        self._width = width


    def weigth(self):
        result = (self._length * self._width * 25 * 5) / 1000
        print(f"Desired mass of materials is {result} tons")


road_1 = Road(20, 5000)
road_1.weigth()
