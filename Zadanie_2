sec = int(input("Введите секунды: "))
h = f"{sec / 3600:.0f}"
m_sec = sec % 3600
m = f"{m_sec / 60:.0f}"
s_sec = m_sec % 60
str(s_sec)
if int(h) < 10:
    h = "0" + h
if int(m) < 10:
    m = "0" + m
if s_sec < 10:
    s_sec = "0" + str(s_sec)
print ((f"{h}:{m}:{s_sec}"))